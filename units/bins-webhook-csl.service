[Unit]
Description=bins-webhook for CSL CS463 reader
After=network.target

[Service]
WorkingDirectory=/home/root/reelyActive/bins-webhook/bin
ExecStart=/usr/local/bin/node bins-webhook-csl
Restart=on-failure
User=reelyactive

[Install]
WantedBy=multi-user.target